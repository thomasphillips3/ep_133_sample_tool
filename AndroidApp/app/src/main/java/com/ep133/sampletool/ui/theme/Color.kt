package com.ep133.sampletool.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Teenage Engineering × Google color palette.
 *
 * Hardware-derived: warm faceplate grays, rubber pad black,
 * signature orange accent, teal wave indicator.
 */
object TEColors {
    // ── Primary accent — EP-133 signature orange ──
    val Orange = Color(0xFFEF4E27)
    val OrangeLight = Color(0xFFFF7E55)
    val OrangeDark = Color(0xFFC03C18)
    val OrangeContainer = Color(0x22EF4E27)

    // ── Secondary — EP-133 teal (wave/online indicator) ──
    val Teal = Color(0xFF00A69C)
    val TealLight = Color(0xFF33C4BB)
    val TealDark = Color(0xFF007A72)
    val TealContainer = Color(0x1F00A69C)

    // ── Faceplate — warm mid-grays from the hardware ──
    val Faceplate = Color(0xFFD1D2D4)
    val FaceplateLighter = Color(0xFFE2E3E5)
    val FaceplateDarker = Color(0xFFC6C7C9)
    val FaceplateButton = Color(0xFFD8D9DB)

    // ── Rubber pad surface ──
    val PadBlack = Color(0xFF222323)
    val PadHighlight = Color(0xFF2D2E2E)

    // ── Text / ink ──
    val Ink = Color(0xFF191A1B)
    val InkSecondary = Color(0xFF555657)
    val InkTertiary = Color(0xFF8A8B8D)
    val InkOnDark = Color(0xFFE2E3E4)
    val InkOnDarkSecondary = Color(0x66E2E3E4)

    // ── Borders ──
    val Border = Color(0xFFB0B1B3)
    val BorderHighlight = Color(0xA6FFFFFF)
}
